# Take as input an integer number; assume the number is between 101 and 999, and that its unit digit is not zero.
# Print the reversed number. For example: 256 -> 652.

n = int(input())
a = n % 10
b = (n // 10) % 10
c = (n // 100) % 10
print(str(a)+str(b)+str(c))
